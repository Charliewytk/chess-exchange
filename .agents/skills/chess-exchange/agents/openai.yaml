interface:
  display_name: "Chess Exchange"
  short_description: "Repo-specific patterns and workflows for chess-exchange"
  default_prompt: "Use the chess-exchange repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true